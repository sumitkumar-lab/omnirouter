from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='The router engine have the capability of multi-loop agentic decision making',
    author='sumit-kumar',
    license='',
)